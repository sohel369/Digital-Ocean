[phases.setup]
nixPkgs = ["python311", "gcc", "postgresql", "file"]

[phases.install]
cmds = ["python3 -m ensurepip", "cd backend && python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
